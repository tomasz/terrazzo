---
"@terrazzo/cli": minor
---

Add `--number-float-names`, `--duration-names`, and `--cubic-bezier-names` to `tz import`. Matchers now respect the Figma resolvedType, propagate one override across an alias chain, and keep Figma types when matchers conflict. `--number-names` is deprecated.
